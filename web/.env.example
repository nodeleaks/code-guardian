# Base URL of the Code Guardian GraphQL API. Must match wherever the
# `app` service from the repo root's docker-compose.yml (or `npm run
# start:dev`) is actually listening.
VITE_API_URL=http://localhost:3000/graphql
